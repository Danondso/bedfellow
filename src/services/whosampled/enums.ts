export enum CONNECTIONS {
  COVERS = 'covers',
  SAMPLES = 'samples',
  SAMPLED_BY = 'sampled_by',
}

export enum HEADER_TITLES {
  CONTAINS_SAMPLES = 'Contains samples',
  COVERED_IN = 'Covered in',
  SAMPLED_IN = 'Sampled in',
}
